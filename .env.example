# 生产环境配置示例
# 复制此文件为 .env 并根据实际情况修改

# JWT 密钥（生产环境必须修改）
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# 环境配置
NODE_ENV=production
PORT=3400

# HTTPS 配置
# 如果使用 HTTPS，设置为 true
HTTPS_ENABLED=false

# Cookie 域名配置
# 如果需要跨子域访问 Cookie，设置为 .yourdomain.com
# 例如：COOKIE_DOMAIN=.example.com
COOKIE_DOMAIN=

# 公共基础 URL（对外访问的前端地址）
# 生产环境设置为你的域名，例如：PUBLIC_BASE_URL=http://your-domain.com:1122
PUBLIC_BASE_URL=

# ---- 教程 RAG / LLM 配置（可选）----
# 用于“教程 + AI 搜索/问答”功能，如不需要可留空
# 一般为 OpenAI / 硅基流动等兼容接口，例如：
#   RAG_API_BASE=https://api.siliconflow.cn/v1
#   RAG_API_KEY=sk-xxxx
#   RAG_LLM_MODEL=deepseek-ai/DeepSeek-V3.2-Exp
#   RAG_EMBED_MODEL=BAAI/bge-m3
#   RAG_EMBED_DIM=1024
RAG_API_BASE=
RAG_API_KEY=
RAG_LLM_MODEL=
RAG_EMBED_MODEL=
RAG_EMBED_DIM=1024

# ---- Agent 生成配置（可选，默认复用 RAG 配置）----
# 如果单独配置网关/API Key，请在此处填写；否则沿用 RAG_API_BASE / RAG_API_KEY
AGENT_API_BASE=
AGENT_API_KEY=
AGENT_MODEL=moonshotai/Kimi-K2-Thinking
